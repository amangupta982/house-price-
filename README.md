# 🏡 House Price Prediction using TensorFlow

This project is a **machine learning web app** built with **Streamlit** and **TensorFlow** that predicts house prices based on features like bedrooms, bathrooms, and size.  
It also includes a **Flask-based REST API** for programmatic predictions.

---

## 🚀 Features
- Upload your CSV dataset with a `price` column.
- Train a TensorFlow regression model directly in the browser.
- Predict house prices for new data points.
- Deploy both:
  - A **Streamlit web app** (UI)
  - A **Flask web API** (backend)

---

## 📁 Project Structure
📦 house-price-predictor
├── app.py               # Streamlit Web App
├── api.py               # Flask API for predictions
├── requirements.txt     # Python dependencies
├── your_dataset.csv     # Example dataset (optional)
└── README.md

---

## 🧠 Technologies Used
- **Python 3.9+**
- **TensorFlow / Keras**
- **Streamlit**
- **Flask**
- **Pandas**, **NumPy**
- **Scikit-learn**
